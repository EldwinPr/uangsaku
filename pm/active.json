{
  "active_issue": null,
  "note": "RUNNABLE BACKLOG COMPLETE 2026-08-24, INCLUDING ONE OWNER-REQUESTED ADDITION. DONE: FEAT01, UC13, UC11, UC14, UC02-add-account, UC01-balance-sheet, UC10-debt-progress, UC04-record-money-movement, UC09-review-and-correct, UC12-budget-consumption, UC03-adjust-account, UC02B-edit-account, FEAT02-navigation-host (128 tests green). Both open questions (Q3 soft delete, Q4 fixed-side+signed-amount adjustment) answered 2026-08-23; UC02B closed 2026-08-24 with the project's first schema change. CI was found broken after that push - a real _slugdir bug in audit.py (glob() enumeration order let UC02-add-account's folder lookup silently resolve to UC02B-edit-account's on Linux/CI but not locally) - fixed and confirmed green via the actual CI log, not local reproduction alone (see lessons.md S5's sixth instance). The owner then asked directly, in this session, for a navigation host so the app could actually be tried - FEAT02-navigation-host closed the same day: AppShell (app/lib/src/app.dart), a 4-tab NavigationBar over an IndexedStack, with every other screen reached contextually. Resolves pm/findings.md F8. NOTHING IS RUNNABLE - every tracker issue is DONE, including this addition. NEXT: none identified. If the owner wants more, the next likely candidates are: a dedicated entry point for UC-03's adjust flow (deliberately left unrouted this time), or ruling on one of the remaining open findings (F7 non-numeric-amount-saves-as-zero, F9 unused codegen toolchain, F11/F12 stale docs, F15 copy-pasted date formatter). NOTES carried forward: (1) sequence .mmd sources are NOT persisted; (2) sequence renders live in their OWNING ISSUE's folder; (3) renders.lock can go stale silently after an as-built XML edit - re-run `python audit.py --record-renders` at close; (4) map.yaml's ucs: lists must be real workbook UC codes only, never an issue id like UC-02B; (5) IndexedStack-based navigation means every FloatingActionButton across the whole app needs an explicit unique heroTag - Flutter's implicit default tag collides once multiple tabs/pushed screens are mounted simultaneously; (6) a clean local audit.py pass (even a real Linux/WSL checkout) does NOT prove CI will pass - an ambiguous glob() match only fails when the actual adverse enumeration order is hit, and only the real CI log surfaces that.",
  "updated": "2026-08-24"
}
