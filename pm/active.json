{
  "active_issue": "FEAT01-foundation",
  "note": "READY FOR THE UNATTENDED RUN. Start it with the /start-dev-pipeline slash command (.claude/commands/start-dev-pipeline.md), which carries the orchestrator brief. Read context/guide/orchestration.md for the loop - the main session ORCHESTRATES ONLY (select -> feat-planner -> flutter-coder -> issue-qa -> select) and does no planning, coding or reviewing itself; if you find yourself editing a .dart file the loop has collapsed and the context isolation is gone. Retry budget is two attempts then halt; halting is per-issue and the backlog has two independent chains, so keep going until nothing is runnable. Only issue-qa commits - a commit before review puts unreviewed code in history. THEN PHASE 2, ONCE: dispatch repo-qa twice in parallel (scope APP over the code as a whole, scope TRAIL over the paper trail), both writing to pm/findings.md - and THE RUN STOPS THERE. Do not re-enter the loop, do not reopen a closed issue, do not create an issue for a finding, do not dispatch flutter-coder at one. Findings are recorded, not fixed: a cross-cutting finding usually needs a decision only the owner can make, and a run that repairs its own findings loops indefinitely with each pass generating the next. A halted issue likewise stays halted for the whole run. Then read pm/log.md's current-state block - it is this project in one screen - and context/index/lessons.md, which is how this project actually goes wrong. START AT FEAT01 STEP 2, NOT STEP 1: the plan is CONFIRMED by the owner, and step 1 (flutter create) already ran on 2026-08-21. app/ exists with name: uangsaku and applicationId com.eldwinpr.uangsaku, android + ios, and is COMMITTED AND PUSHED. It carries no build_runner or drift dependency yet - that is step 2. UNATTENDED MODE IS LIVE (context/general-rules.md, decisions.md 2026-08-21): feat-planner may mark a plan AUTO-CONFIRMED only when EVERY decision in it cites an artifact the owner already confirmed - a stated FR, the sequence diagram, a class diagram, decisions.md, enums.md, or the workbook row. A decision that cannot be cited HALTS that issue and the question goes to pm/questions.md; halting is per-issue, so the two independent chains (UC14->UC02->UC01/UC03/UC10, and UC13/UC11->UC04->UC09/UC12) do not block each other. THE FAILURE THIS MODE MAKES POSSIBLE IS OVER-PERMISSION, NOT OVER-HALTING: a wrongly halted plan costs a round trip, a wrongly self-confirmed one becomes code nobody reviewed. Halt on doubt. NO OPEN RULINGS - both that stood this morning were answered 2026-08-21 and are recorded in decisions.md: budget group CRUD moved from UC-13 to UC-11 (the module owning Budget_Group; workbook rows, class-budgeting.drawio, seq-uc11 and seq-uc13 all updated), and the free-text note appears on EVERY recording screen, not just expense and income (seq-uc06/07/08 redrawn). TEN PLANS REMAIN UNWRITTEN - every issue except FEAT01 is still a NOT PLANNED placeholder, and feat-planner writes them as part of this run. TOOLCHAIN: Flutter 3.47.1 / Dart 3.13.1 at C:/flutter, matching the CI pin; a session started before the PATH edit cannot see flutter and must be RESTARTED, not re-PATHed. NO RUNNABLE TARGET: no Android SDK (install before UC14, the first UI issue), no Mac so app/ios/ is versioned but never compiled, and web is not a fallback because NativeDatabase.createInBackground is native-only. flutter test is headless and works - that is where testing.md puts the correctness surface, so every issue can be built and tested; only launching and looking waits. CI: two jobs, both live and both running on every push. docs runs audit.py. app runs pub get / format / analyze / test on app/, plus build_runner GATED SEPARATELY on build_runner actually being in pubspec.yaml - it failed once on the bare scaffold because the old guard checked whether a pubspec existed rather than whether there were builders to run, and the guard was fixed rather than the commit reverted. All five steps are verified green on the current scaffold, so from here a red build means YOUR change broke something. THE CODING CONVENTIONS ARE STILL PROVISIONAL: pub get has now run and resolved, but no drift/riverpod/build_runner version is pinned or compiled yet, so no version number in riverpod.md or drift.md is verified. FEAT01 is expected to correct them and say so in pm/log.md. audit.py green at 13/0/0.",
  "updated": "2026-08-21"
}
