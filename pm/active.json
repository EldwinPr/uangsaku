{
  "active_issue": "FEAT04-nav-redesign",
  "note": "TRACKED BACKLOG COMPLETE 2026-08-24 (FEAT01 through FEAT03, 22 tracker issues DONE). A manual-testing feedback round is now under way, outside the workbook/UC-traced backlog - four items from the owner's first hands-on test pass, sequenced because the first touches every screen's strings: (1) FEAT03-settings-and-i18n DONE 2026-08-24 - full EN/ID i18n, Settings hub (currency/language/theme mode/theme color), third schema change (schemaVersion 2->3, Settings.locale/themeMode/seedColor). (2) FEAT04-nav-redesign NEXT - not yet planned: 5-tab bottom nav (add a dedicated Accounts tab, rename Balance Sheet to something concise, center Record as a colored circular quick-action button). (3) category picker -> autocomplete/combobox with inline 'type new to create' (UI only, no schema change - Category/Subcategory stay two tables). (4) save-flow UX - auto-close/confirm on save so double-tapping Save doesn't create duplicates and the user gets feedback the save happened; floppy-disk save icon replaced with something more recognizable; account-name uniqueness (case-insensitive, warn-but-still-allow-save, matching the app's established warn-never-refuse pattern, NOT a hard block per NFR-4). NOTES carried forward: (1) sequence .mmd sources are NOT persisted; (2) sequence renders live in their OWNING ISSUE's folder; (3) renders.lock can go stale silently after an as-built XML edit - re-run `python audit.py --record-renders` at close; (4) map.yaml's ucs: lists must be real workbook UC codes only, never an issue id; (5) IndexedStack-based navigation means every FloatingActionButton needs an explicit unique heroTag; (6) a clean local audit.py pass does NOT prove CI will pass - only the actual CI log surfaces a glob()-ordering-dependent bug; (7) this project is a pilot run for a different target project (owner's own words, 2026-08-24) - the process trail itself (this log, the close checklist, how ad-hoc requests get tracked) is part of what gets extracted later, so keep the same discipline on these post-backlog polish items as on the original tracked issues, even though none of them trace to a UC/workbook row.",
  "updated": "2026-08-24"
}
