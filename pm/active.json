{
  "active_issue": "FEAT06-save-ux-and-uniqueness",
  "note": "TRACKED BACKLOG COMPLETE (FEAT01 through FEAT03, 22 tracker issues DONE). Manual-testing feedback round under way, outside the workbook/UC-traced backlog: (1) FEAT03-settings-and-i18n DONE 2026-08-24. (2) FEAT04-nav-redesign DONE 2026-08-24. (3) FEAT05-category-picker DONE 2026-08-24 - category/subcategory pickers are now autocomplete-with-inline-create, UI only, no class diagram change needed (private per-file widget). A real setState-during-build bug was caught and fixed by the coder during implementation. (4) FEAT06-save-ux-and-uniqueness NEXT, not yet planned - the last item, covering three related sub-asks from the owner's original feedback: (a) auto-close/confirm on save so double-tapping Save doesn't create duplicates and the owner gets feedback the save happened - applies to every screen with a save action (AccountFormScreen create/edit/adjust, RecordTransactionScreen, TransactionListScreen's edit sheet, SetBudgetScreen, CategoryManagerScreen's add/rename dialogs); (b) the floppy-disk save icon (Icons.save) replaced with something more recognizable to a newer generation - likely Icons.check or Icons.done, check every FAB/button using Icons.save first; (c) account-name uniqueness - case-insensitive, warn-but-still-allow-save (matching the app's established warn-never-refuse pattern, e.g. currency-relabel notice - NOT a hard block, NFR-4 zero refusals). This closes the owner's manual-testing feedback round once done. NOTES carried forward: (1) sequence .mmd sources are NOT persisted; (2) sequence renders live in their OWNING ISSUE's folder; (3) renders.lock can go stale silently after an as-built XML edit - re-run `python audit.py --record-renders` at close; (4) map.yaml's ucs: lists must be real workbook UC codes only, never an issue id; (5) IndexedStack-based navigation means every FloatingActionButton needs an explicit unique heroTag - six across the app as of FEAT04, check for collisions before adding more; (6) a clean local audit.py pass does NOT prove CI will pass - only the actual CI log surfaces a glob()-ordering-dependent bug (fixed 2026-08-23); (7) BEFORE writing a new screen/class, check the relevant class diagram has a box for it - flutter-coder halting on FEAT04's AccountsScreen is the model to follow; a genuinely private per-file widget (like FEAT05's _CategoryAutocompleteField) does NOT need a diagram entry - check whether the new thing is a tracked class (Screen/Provider/DAO/Notifier) or private implementation detail before halting; (8) this project is a pilot run for a different target project (owner's own words, 2026-08-24) - the process trail itself is part of what gets extracted later, so keep the same discipline on these post-backlog polish items as on the original tracked issues.",
  "updated": "2026-08-24"
}
