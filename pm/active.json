{
  "active_issue": null,
  "note": "NEXT ACTION: write the 11 plan.md files. Everything else the pipeline needs is in place. No code before a plan.md exists AND is user-confirmed - and for the unattended run all 11 must be confirmed up front, which is the whole reason the owner is writing them first. TOOLCHAIN: Flutter 3.47.1 / Dart 3.13.1 stable at C:/flutter, on the user PATH; Dart MCP server 1.1.1 connected, registered by ABSOLUTE path (bare 'dart' fails from a session started before the PATH edit; the server's first launch prints pub output onto stdout and breaks the handshake exactly once - both written up in context/coding-conventions/tooling.md). NO RUNNABLE TARGET YET: no Android SDK, Visual Studio incomplete, and web is NOT a fallback because NativeDatabase.createInBackground is native-only. flutter test is headless and works, which is where testing.md puts the correctness surface, so every issue can be built and tested - only launching-and-looking waits. Install the Android SDK before the first UI issue, not before FEAT01. GIT + CI: EldwinPr/uangsaku on GitHub, main. .github/workflows/ci.yml has two jobs - docs runs audit.py today, app runs build_runner/format/analyze/test and is guarded on pubspec.yaml so it stays inert until FEAT01. audit.py green at 13/0/0, also verified on a fresh clone. DOCUMENTATION DONE: all 14 per-use-case sequence diagrams (docs/diagrams/seq-uc01..uc14.drawio; renders committed to pm/issues/<issue>/, ERD+class+component renders stay in docs/diagrams and stay gitignored). Every lifeline script-checked against the class diagrams. Transaction.note added to the ERD. Sequence renders are staleness-checked via docs/diagrams/renders.lock - after ANY re-export run `python audit.py --record-renders`, after the export and never instead of it. TWO OWNER RULINGS OUTSTANDING, full reasoning in pm/log.md: (1) UC-13 step 3, budget group CRUD, has no supporting class on any class diagram - seq-uc13 scopes it out with a note rather than inventing a participant, so UC-13 as drawn does not deliver what the workbook promises; either a class diagram gains the classes or UC-13 is re-sliced. (2) Does the note field appear on the transfer / lend-borrow / repayment screens? seq-uc04 and seq-uc05 carry it, seq-uc06/07/08 do not; the column exists on Transaction either way. ALSO PENDING: the Dart package name and app id must be settled before FEAT01 runs flutter create (repo is named uangsaku); one fr-nfr.md section 4 item left (where the data lives - narrowed to phone-only, not closed); .github/ and renders.lock were still uncommitted when this was written. THE CODING CONVENTIONS ARE STILL PROVISIONAL: the SDK exists now, but no pub get has run and no package version is verified. FEAT01 is expected to correct them and say so in pm/log.md.",
  "updated": "2026-08-21"
}
