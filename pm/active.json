{
  "active_issue": "FEAT01-foundation",
  "note": "PLAN CONFIRMED 2026-08-21, implementation not started. pm/issues/feat01-foundation/plan.md is a real plan (nine decisions, twelve steps, out-of-scope list) - it was a NOT PLANNED placeholder that morning. NEXT ACTION: step 1, run from the REPO ROOT - `flutter create --project-name uangsaku --org com.eldwinpr --platforms=android,ios app`. Name the target dir explicitly, never '.': this was run once in C:/Users/cg857 from a terminal opened in the wrong place and scattered twelve items across the home directory (cleaned; flutter create skips an existing .gitignore/README.md and does not git init, which is what made that safe). TWO DECISIONS LANDED TODAY, both in decisions.md and pm/log.md: (1) iOS is a CONFIRMED target, which closes the assumption the entire Flutter-over-Kotlin choice was provisional on - the stack decision is now settled and the Kotlin entry permanently superseded. iOS cannot be built on this machine (Apple toolchain is macOS-only), so app/ios/ is generated and committed but never compiled, tested or covered by CI; a green Android build says nothing about it. (2) The Dart package lives in app/, NOT the repo root. That move silently broke CI - the app job's probe was guarded on a root pubspec.yaml that will now never exist, so it would have reported success having run nothing, forever. Fixed: probe reads app/pubspec.yaml, every Flutter step carries working-directory: app. DOCS UPDATED TO MATCH: decisions.md (two new entries), fr-nfr.md (stack caveat retired, iOS note added), dart-and-flutter.md (layout gains the app/ prefix), tooling.md (Xcode row + the iOS-is-unbuildable-here section), ci.yml, and the plan's D2/D4/D9 plus step 1. audit.py green at 13/0/0 after all of it. TOOLCHAIN: Flutter 3.47.1 / Dart 3.13.1 at C:/flutter, matching the CI pin. The PATH trap in tooling.md is real - a session started before the PATH edit cannot see flutter/dart and must be restarted, not re-PATHed. Still NO RUNNABLE TARGET: no Android SDK (install before UC14, the first UI issue, not before FEAT01), no Mac, and web is not a fallback because NativeDatabase.createInBackground is native-only. flutter test is headless and works, which is where testing.md puts the correctness surface. NOTHING IS COMMITTED and app/ is empty. Run FEAT01 D7's four commands (build_runner, format, analyze, test) BEFORE the first push - landing app/pubspec.yaml is what switches the CI job on, and a red build on the commit that starts implementation is avoidable. THE CODING CONVENTIONS ARE STILL PROVISIONAL: no pub get has run for this project, so every version number in riverpod.md is shape, not fact. FEAT01 is expected to correct them and say so in pm/log.md. STILL OPEN, neither gating FEAT01 because both are about screens and FEAT01 builds none: UC-13 step 3's budget group CRUD has no supporting class on any class diagram, and whether the note field appears on the transfer / lend-borrow / repayment screens (seq-uc04/05 carry it, seq-uc06/07/08 do not). One fr-nfr.md section 4 item also remains - where the data lives, narrowed to phone-only but not closed.",
  "updated": "2026-08-21"
}
