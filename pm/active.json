{
  "active_issue": "UC02-add-account",
  "note": "THE RUN RESUMED 2026-08-22: the owner answered Q1 and told the session to finish all issues. DONE: FEAT01-foundation (2026-08-21), UC13-categories (2026-08-21), UC11-set-budget (2026-08-22), UC14-choose-currency (2026-08-22). All committed and pushed. NO HALTS OUTSTANDING. Q1 IS ANSWERED (2026-08-22): changing the currency 'just changes the prefix thats all' - it re-labels, the stored int minor units are untouched, and the exponent is a rendering concern. That unblocked UC14 and the seven issues behind it. NEXT: UC02-add-account. ITS PLAN IS NOT WRITTEN. Remaining chain: UC02 -> UC03/UC01/UC10 -> UC04 -> UC09/UC12, seven issues. THE STATE OF app/: a Dart project holding the seven ERD tables at schemaVersion 1 under app/lib/src/{accounts,transactions,budgeting,settings}, AppDatabase on a background isolate via drift_flutter's driftDatabase(), the committed v1 schema snapshot, one seeded Settings row at IDR. Three modules are built end to end - UC-13's CategoryDao/categoryTreeProvider/CategoriesNotifier/CategoryManagerScreen, UC-11's BudgetDao/BudgetNotifier/budgetProvider/Clock/SetBudgetScreen, and UC-14's SettingsDao/currencyProvider/SettingsNotifier/CurrencyScreen, which is what MaterialApp.home points at temporarily (FR-1 gives that spot to UC01). 37 tests green. WARNING - F8: home is the only route to a screen, so each screen issue orphans the last; CategoryManagerScreen and SetBudgetScreen are both unreachable now and five more screens are queued. DO NOT RE-SCAFFOLD AND DO NOT RE-RUN flutter create. WHAT COMES NEXT IS PHASE 2, ONCE: dispatch repo-qa twice in parallel, scope APP over the code as a whole and scope TRAIL over the paper trail, both writing to pm/findings.md - AND THE RUN STOPS THERE. Do not re-enter phase 1, do not reopen a closed issue, do not create an issue for a finding, do not dispatch a coder at one. Findings are recorded, not fixed: a cross-cutting finding usually needs a decision only the owner can make, and a run that repairs its own findings loops indefinitely with each pass generating the next. FINDINGS ON FILE: F1 (the table-existence test reads drift's declared allTables rather than sqlite_master), F2 (FIXED), F3 (twelve sequence diagrams still name NativeDatabase.createInBackground where the code uses createBackgroundConnection - same guarantee, stale name; UC-13 and UC-11 each fixed their own, the rest get corrected by their own as-built passes), F4 (the NFR-4 enabled-controls test could pass vacuously for the rename control - UC-11's version asserts findsNWidgets first and does not have this hole), F5 (docs/workbook.xlsx has no column to mark a use case implemented, so the close checklist's step 4 cannot be performed as written), F6 (FIXED - a long note over renders outside its own box), F7 (a non-numeric budget amount is silently saved as zero; NFR-4 forbids refusing, so which non-refusing behaviour is wanted is the OWNER'S call, and the same parse will be needed by every amount field UC-04 introduces). OWNER QUESTIONS STILL UNANSWERED, none blocking: is com.eldwinpr.uangsaku the right application id; does the moneytracker/uangsaku split bother you; there is no navigation host so one screen is reachable at a time; deleting a category silently blanks the tag on every transaction that used it; category ordering is insertion order. OWNER'S DIRECTION MID-RUN (2026-08-22): DIAGRAM WORK AND QA NO LONGER GO TO SUBAGENTS - the main session does the as-built diagram passes and the review-and-close itself; planning and coding still dispatch to feat-planner and flutter-coder. context/guide/orchestration.md still describes the older three-agent loop. TOOLCHAIN: Flutter 3.47.1 / Dart 3.13.1 at C:/flutter, matching the CI pin; a session started before the PATH edit cannot see flutter and must be RESTARTED. flutter is at C:/flutter/bin/flutter and is not on the shell PATH. flutter/dart commands run with working-directory app. NO RUNNABLE TARGET: no Android SDK and no Mac - flutter test is headless and is where testing.md puts the correctness surface; two whole screens have been built without ever launching one. THE CONVENTIONS HAVE NOW BEEN CORRECTED BY THREE CONSECUTIVE ISSUES, always in the same direction - the real toolchain overrules a convention written before any code existed: FEAT01 (driftDatabase() replaces the literal NativeDatabase.createInBackground), UC13 (a DAO drawn with update()/delete() CANNOT be a DatabaseAccessor - invalid_override against DatabaseConnectionUser, so compose AppDatabase and expect no daos: entry; and riverpod_generator throws InvalidTypeException on ANY provider typed over a drift-generated row class, so hand-write those), UC11 (a screen reading MORE THAN ONE drift stream needs a plain Notifier with hand-opened subscriptions cancelled in ref.onDispose - a StreamNotifier over a combining StreamController never releases them and hangs AppDatabase.close()). All are in context/index/decisions.md and bind AccountDao and TransactionDao too. STILL UNVERIFIED BY ANY CODE: a cross-module join and a derived-figure query - both arrive with UC01. audit.py green at 13/0/0; 14 sequence renders recorded in docs/diagrams/renders.lock.",
  "updated": "2026-08-22"
}
