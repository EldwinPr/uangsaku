{
  "active_issue": "UC14-choose-currency",
  "note": "UNATTENDED RUN IN PROGRESS. FEAT01-foundation is DONE and committed (2026-08-21) - the repo is now a Dart project: app/ holds the seven ERD tables at schemaVersion 1 under app/lib/src/{accounts,transactions,budgeting,settings}, AppDatabase on a background isolate via drift_flutter's driftDatabase(), the committed v1 schema snapshot, one seeded Settings row at IDR, and four passing tests. DO NOT RE-SCAFFOLD AND DO NOT RE-RUN flutter create. NEXT: UC14-choose-currency, chosen because tracker order puts it first among the three rows that depend only on FEAT01 (the others are UC13-categories and UC11-set-budget). It is the FIRST SCREEN, the first DAO and the first Notifier - the cheapest place to discover the class diagrams' Screen -> provider -> DAO -> AppDatabase chain is wrong. ITS PLAN IS NOT WRITTEN: dispatch feat-planner first, and it will be the first plan to use AUTO-CONFIRMED for real. Read context/guide/orchestration.md for the loop - the main session ORCHESTRATES ONLY (select -> feat-planner -> flutter-coder -> issue-qa -> select) and does no planning, coding or reviewing itself; if you find yourself editing a .dart file the loop has collapsed. Retry budget is two attempts then halt; halting is per-issue and the backlog has two independent chains (UC14->UC02->UC01/UC03/UC10, and UC13/UC11->UC04->UC09/UC12), so keep going until nothing is runnable. Only issue-qa commits. THEN PHASE 2, ONCE: dispatch repo-qa twice in parallel (scope APP over the code, scope TRAIL over the paper trail), both writing to pm/findings.md - and THE RUN STOPS THERE. Do not re-enter the loop, do not reopen a closed issue, do not create an issue for a finding. AUTO-CONFIRMED means EVERY decision in the plan cites an artifact the owner already confirmed - a stated FR, the sequence diagram, a class diagram, decisions.md, enums.md, or the workbook row; anything else HALTS that issue into pm/questions.md. THE FAILURE THIS MODE MAKES POSSIBLE IS OVER-PERMISSION, NOT OVER-HALTING. Halt on doubt. NO OPEN RULINGS. Two questions are the OWNER'S and are still unanswered, neither blocking: is com.eldwinpr.uangsaku the right application id, and does the moneytracker/uangsaku split bother you (the repo directory is moneytracker, the package and the class-diagram-free artifacts are uangsaku; FEAT01 deliberately renamed nothing). ONE FINDING ON FILE: pm/findings.md F1, the table-existence test reads drift's declared allTables rather than the SQLite schema - recorded, not fixed. TOOLCHAIN: Flutter 3.47.1 / Dart 3.13.1 at C:/flutter, matching the CI pin; a session started before the PATH edit cannot see flutter and must be RESTARTED. flutter/dart commands run with working-directory app. NO RUNNABLE TARGET: no Android SDK - install it before UC14 if the app is to be LAUNCHED, but flutter test is headless and needs none, and that is where testing.md puts the correctness surface. No Mac, so app/ios/ is versioned and never compiled. CONVENTIONS ARE NOW HALF-VERIFIED: FEAT01 corrected riverpod.md (real versions, riverpod is at major 4 not 3), drift.md (driftDatabase() wraps NativeDatabase.createBackgroundConnection) and dart-and-flutter.md (constant_identifier_names: false, so enum values stay spelled as docs/enums.md spells them). Everything ABOVE the database - DAOs, notifiers, screens, widget tests - is still unverified shape. audit.py green at 13/0/0.",
  "updated": "2026-08-21"
}
