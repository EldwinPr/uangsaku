{
  "active_issue": "FEAT05-category-picker",
  "note": "TRACKED BACKLOG COMPLETE (FEAT01 through FEAT03, 22 tracker issues DONE). Manual-testing feedback round under way, outside the workbook/UC-traced backlog: (1) FEAT03-settings-and-i18n DONE 2026-08-24 - full EN/ID i18n, Settings hub, third schema change. (2) FEAT04-nav-redesign DONE 2026-08-24 - 5-tab bottom nav (new Accounts tab split out of BalanceSheetScreen, Home renamed, Record as a colored circular FAB docked in a BottomAppBar notch). flutter-coder correctly halted its first FEAT04 pass because class-accounts.drawio had no AccountsScreen box - fixed in the main session (commit 04ba8b0), same agent resumed to finish. (3) FEAT05-category-picker NEXT, not yet planned: Category/Subcategory picker becomes an autocomplete/combobox - pick existing or type a new name to create it inline. UI ONLY, no schema change - Category/Subcategory stay two separate drift tables (owner explicitly chose this scope over collapsing them, 2026-08-24). Touches RecordTransactionScreen's and TransactionListScreen's edit-sheet category/subcategory pickers. (4) After that: save-flow UX - auto-close/confirm on save so double-tapping Save doesn't create duplicates and the user gets feedback the save happened; floppy-disk save icon replaced with something more recognizable; account-name uniqueness (case-insensitive, warn-but-still-allow-save, matching the app's established warn-never-refuse pattern, NOT a hard block per NFR-4). NOTES carried forward: (1) sequence .mmd sources are NOT persisted; (2) sequence renders live in their OWNING ISSUE's folder; (3) renders.lock can go stale silently after an as-built XML edit - re-run `python audit.py --record-renders` at close; (4) map.yaml's ucs: lists must be real workbook UC codes only, never an issue id; (5) IndexedStack-based navigation means every FloatingActionButton needs an explicit unique heroTag - now six across the app, check this list doesn't collide when adding a seventh; (6) a clean local audit.py pass does NOT prove CI will pass - only the actual CI log surfaces a glob()-ordering-dependent bug (fixed 2026-08-23, but the general lesson stands for any future filesystem-order-dependent code); (7) BEFORE writing a new screen/class, check the relevant class diagram has a box for it - flutter-coder halting on FEAT04's AccountsScreen is the model to follow, not an exception; (8) this project is a pilot run for a different target project (owner's own words, 2026-08-24) - the process trail itself is part of what gets extracted later, so keep the same discipline on these post-backlog polish items as on the original tracked issues.",
  "updated": "2026-08-24"
}
